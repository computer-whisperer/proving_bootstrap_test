//! M3 — in-place array reverse over a mutable memory, proven equal to the
//! functional `rev`. This file is just the test-binary entry point; the proofs
//! and their tests live in the `common` submodules (see `common/mod.rs` for the
//! reading order and `docs/M3-WALKTHROUGH.md` for the narrative).

mod common;
